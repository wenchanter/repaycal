**Note**
this project uses etcd and postgresql, so it will start a etcd and postgresql instance in Docker or k8s

**docker run**  
sudo ./docker_run.sh

**k8s run**  
sudo ./k8s_run.sh   
Note: this shell has only been tested on a local Docker Desktop's k8s cluster

